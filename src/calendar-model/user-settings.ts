/**
 * Specifies how to compute the set of leinings.
 *
 * These settings are stored in localStorage by the UI layer.
 */
export interface UserSettings {
  israel: boolean
  includeModernHolidays: boolean
  ashkenazi: boolean
}
