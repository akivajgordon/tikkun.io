const hebrewNumeralFromInteger = require('./src/hebrew-numeral')
const textFilter = require('./src/text-filter')
const displayRange = require('./src/display-range')

window.TikkunApp = window.TikkunApp || {textFilter, hebrewNumeralFromInteger, displayRange}
